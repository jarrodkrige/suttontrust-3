# Submission from Ibukun

> Team 3 · Role software-developer-2 · 2026-05-08 14:12

:root {
  --bg: #FFFDF8;
  --text: #1F2937;

  --primary: #2563EB;
  --accent: #FF7A18;
  --green: #22C55E;

  --card-bg: #FFFFFF;
  --border: #E5E7EB;
}

/* Page */
body {
  background: var(--bg);
  color: var(--text);
}

/* Section spacing (bigger = more modern) */
section {
  padding: 80px 20px;
}

/* Cards */
.card {
  background: var(--card-bg);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 24px;
  transition: all 0.2s ease-out;
}

.card:hover {
  transform: translateY(-6px);
  box-shadow: 0 12px 30px rgba(0,0,0,0.08);
}

/* Buttons */
button, .btn {
  background: var(--accent);
  color: white;
  border-radius: 999px;
  padding: 14px 22px;
  font-weight: 600;
  transition: all 0.2s ease-out;
}

button:hover {
  transform: scale(1.04);
}

/* Numbers section pop */
.stat-number {
  color: var(--primary);
  font-size: 2.5rem;
}
# Submission from Ibukun

> Team 3 · Role software-developer-2 · 2026-05-08 14:20

Perfect — you’re basically asking to shift this from “academic charity site” → “modern youth-friendly product (Squarespace / Notion / Stripe vibes)”
Here’s a clean, pasteable design system + CSS upgrade to completely change the vibe 👇
🎨 1. New “cool modern pastel” colour system
Use this — it’s soft, modern, and still trustworthy:
:root {
  /* Base */
  --bg: #FAFAFF;
  --surface: #FFFFFF;
  --text: #111827;
  --muted: #6B7280;
  --border: #E5E7EB;

  /* Pastel accents */
  --blue: #7C9CF5;
  --purple: #C4B5FD;
  --pink: #F9A8D4;

  /* Gradients */
  --gradient-main: linear-gradient(135deg, #7C9CF5, #C4B5FD, #F9A8D4);

  /* Shadows */
  --shadow-soft: 0 10px 30px rgba(0,0,0,0.06);
  --shadow-hover: 0 14px 40px rgba(0,0,0,0.1);
}
👉 Instantly moves you away from “corporate navy + orange” into something fresh

🧱 2. Global layout feel (this is key)
Squarespace vibe =
✅ more whitespace
✅ softer edges
✅ floating elements (not boxed in)body {
  background: var(--bg);
  color: var(--text);
  font-family: Inter, system-ui, sans-serif;
  line-height: 1.6;
}

section {
  padding: 100px 24px;
  max-width: 1100px;
  margin: 0 auto;
}🧊 3. Cards → make EVERYTHING feel like floating UI
Right now your cards are dull boxes.
Replace with:.card {
  background: var(--surface);
  border-radius: 20px;
  padding: 28px;
  box-shadow: var(--shadow-soft);
  border: 1px solid var(--border);
  transition: all 0.25s ease;
}

.card:hover {
  transform: translateY(-6px) scale(1.01);
  box-shadow: var(--shadow-hover);
}
👉 This alone makes it feel like a modern app

✨ 4. Add subtle gradients (this is the vibe shift)
Hero accent:.hero-title span {
  background: var(--gradient-main);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}Use like:<h1>
  Not sure if <span>uni is right</span> for you?
</h1>Section divider glow:.section-accent {
  position: relative;
}

.section-accent::before {
  content: "";
  position: absolute;
  width: 300px;
  height: 300px;
  background: var(--gradient-main);
  filter: blur(80px);
  opacity: 0.15;
  top: -100px;
  left: -100px;
}👉 Gives that “soft pastel glow” look

🔘 5. Buttons → modern pill style (IMPORTANT).btn {
  background: var(--gradient-main);
  color: white;
  padding: 14px 24px;
  border-radius: 999px;
  font-weight: 600;
  border: none;
  transition: all 0.2s ease;
}

.btn:hover {
  transform: scale(1.05);
  box-shadow: 0 10px 25px rgba(124,156,245,0.35);
}👉 Much more “cool app” than flat orange boxes

🟦 6. Stats section → colourful cards
Replace boring stat cards with tinted backgrounds:.stat-card:nth-child(1) {
  background: #EEF2FF;
}

.stat-card:nth-child(2) {
  background: #F5F3FF;
}

.stat-card:nth-child(3) {
  background: #FDF2F8;
}

.stat-number {
  font-size: 2.8rem;
  font-weight: 700;
}
``👉 Adds colour without being loud

🧑‍🤝‍🧑 7. People sections (voices + buddy)
Make them feel like social profiles:.avatar {
  width: 64px;
  height: 64px;
  border-radius: 50%;
  object-fit: cover;
}

.profile-card {
  display: flex;
  gap: 16px;
  align-items: center;
}
Show more lines
👉 This removes the “brochure” feel immediately

🌊 8. Background layering (very important)
Add soft depth:
body::before {
  content: "";
  position: fixed;
  width: 600px;
  height: 600px;
  background: radial-gradient(circle, #C4B5FD, transparent);
  top: -200px;
  right: -200px;
  opacity: 0.2;
  z-index: -1;
}
``🧠 9. Typography tweak (quick win)
Keep Playfair (nice), but tighten it:h1, h2 {
  letter-spacing: -0.02em;
}

p {
  color: var(--muted);
  font-size: 1.05rem;
}
👉 Makes it feel more modern instantly

🎯 10. Small but high-impact details
✅ Rounded everythingimg {
  border-radius: 16px;
}✅ Softer bordersborder: 1px solid rgba(0,0,0,0.05);✅ Add spacing between sectionssection + section {
  margin-top: 40px;
}
``🚀 What this transforms your site into
Before:
👉 Charity info page
👉 Static, text-heavy
👉 Safe but forgettable
After:
👉 Feels like a modern product / platform
👉 Youth-friendly and “de